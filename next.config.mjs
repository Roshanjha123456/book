/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com' , 'aceternity.com' , 'assets.aceternity.com'], // Add the image domain here
      },
};

export default nextConfig;
// next.config.js

  